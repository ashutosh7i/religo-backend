{
  "name": "religo-backend",
  "version": "0.0.0",
  "private": true,
  "author": "Ashutosh7i",
  "scripts": {
    "build": "turbo run build",
    "clean": "turbo run clean",
    "predev": "turbo run build --filter=@repo/fastify",
    "dev": "turbo run dev",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "dependencies": {},
  "devDependencies": {
    "prettier": "^3.2.5",
    "turbo": "^2.5.4"
  },
  "engines": {
    "node": ">=14.0.0",
    "npm": ">=7.0.0"
  },
  "packageManager": "npm@10.9.2",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
